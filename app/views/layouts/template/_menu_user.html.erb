<div class="navbar">
  <div class="navbar-inner">
    <div class="container">

      <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>

      <!-- Be sure to leave the brand out there if you want it shown -->
      <a class="brand" href="/">ShareDoc</a>


      <ul class="nav pull-right">
        <li>
          <a class="text-success"><dt class="text-success">Share documents with us</dt></a>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <% if current_user %>
                <%= current_user.email %>
            <% else %>
                Sign up / Login
            <% end %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <% if current_user %>
                <li><a class="btn" type="button" href="#sign_up_modal" data-toggle="modal">Profile</a></li>
                <!--<li>< %= link_to "Profile", profile_path %></li>-->
                <li><a class="btn" type="button" href="/log_out" data-toggle="modal">Logout</a></li>
                <!--<li>< %= link_to "Log out", log_out_path %></li>-->
            <% else %>
                <!--<li>< %= link_to "Log in", log_in_path %></li>-->
                <!--<li>< %= link_to "Sign up", sign_up_path %></li>-->
                <li><a class="btn" type="button" href="#login_modal" data-toggle="modal">Login</a></li>
                <li><a class="btn" type="button" href="#sign_up_modal" data-toggle="modal">Sign up</a></li>
            <% end %>

          </ul>
        </li>

      </ul>

      <!-- Everything you want hidden at 940px or less, place within here -->
      <div class="nav-collapse">
        <ul class="nav">
        <% if current_user %>
            <li class=<%= "active" if current_page == "documents" %>><%= link_to 'Documents', controller: "documents", action: "index" %></li>
            <!--<li>< %= link_to 'Shared Documents', controller: "documents", action: "share" %></li>-->
            <!--<li><a href="#"><span>My Friends</span></a></li>-->
            <li class=<%= "active" if current_page == "friends" %>><%= link_to 'Friends', controller: "friends", action: "index" %></li>
            <!--<li>< %= link_to 'Activities', controller: "activities", action: "index" %></li>-->
        <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>




<%= render 'layouts/template/modal' %>